// looping OOP feature which allows the execution of a code
// repeatedly without having to write the x amount of times

for (let i = 0; i < 5; i++) {
console.log("Good Morning", i);
}

// loop and display even number
for (let i = 0; i < 10; i++) {
    if(i % 2 === 0)
    console.log("Answer =", i);
    }
    

    // loop and display odd number
for (let i = 0; i < 10; i++) {
    if(i % 2 === 1)
    console.log("Answer =", i);
    }

 // loop and display odd number Another way
for (let i = 0; i < 10; i++) {
    if(i % 2 !== 0)
    console.log("Answer =", i);
    }


    // loop and display reverse order
for (let i = 10; i > 0; i--) { 
    console.log("Answer =", i);
    }